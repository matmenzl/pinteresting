<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="col-sm-8 col-md-8">
        <%= search_form_for @q, method: :get, class: "navbar-form" do |f| %>
          <div class="input-group">
            <%= f.text_field :description_cont, class: "form-control", placeholder: "E.g. 'Car Rent'" %>
            <div class="input-group-btn">
              <%= button_tag class: "btn btn-default", type: "submit" do %>
                <i class="glyphicon glyphicon-search"></i>
              <% end %>
            </div>
          </div>
        <% end %>        
      </div>

      <div class="col-sm-3 col-md-3 col-offset-1">
          <div class="btn-group nav navbar-nav navbar-right" role="group">
          <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            Filter by Type
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Offers", filters_path('offer') %></li>
            <li><%= link_to "Requests", filters_path('request') %></li>
          </ul>
          </div>
      </div>
    </div>
</nav>