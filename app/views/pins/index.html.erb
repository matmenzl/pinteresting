<%= javascript_tag do %>
  window.map_markers = <%== @markers.to_json %>;
<% end %>

<%= render 'pages/home' unless user_signed_in? %>

<%= render 'search_menu' %>

<%= render 'map' if (user_signed_in? && params[:q].try(:[], :description_cont).nil?) %>

<%= render "shared/pins", pins: @pins %>
