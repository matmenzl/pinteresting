<nav class="navbar navbar-static-top navbar-default" role="navigation">
  <div class="container">
  <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t("header.brand"), root_path, class: "navbar-brand" %>
    </div><!-- /.navbar-header -->

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t("header.links.home"), root_path %></li>
        <li><%= link_to t("header.links.about"), about_path %></li> 
        <% if user_signed_in? %>
          <li>
            <button type="button" class="btn btn-default navbar-btn">
              <%= link_to new_pin_path do %>
                  <span class="glyphicon glyphicon-plus"></span> <%= t("header.links.new_pin") %>
              <% end %>
            </button>
          </li>
          <li><%= link_to t("header.links.edit_account"), edit_user_registration_path %></li>
          <li><%= link_to t("header.links.logout"), destroy_user_session_path, method: :delete %></li> 
        <% else %>
          <li><%= link_to t("header.links.login"), new_user_session_path %></li> 
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>